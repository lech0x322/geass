{
  "buildCommand": "cd $(git rev-parse --show-toplevel) && pnpm --filter @geass/web build",
  "outputDirectory": ".next",
  "installCommand": "cd $(git rev-parse --show-toplevel) && pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
